---
- hosts: all
  become: true
  pre_tasks:
    - name: Install python for Ansible
      raw: test -e /usr/bin/python3 || (yum install -y python3)
      changed_when: false
  roles:
  - role: gepardec.ntp
  - role: gepardec.docker
    vars: 
      docker_users:
      - vagrant
    